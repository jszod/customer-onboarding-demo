# Recorded fixtures

Recorded from a live run by `make fixtures` (§16.7). **Never edit by hand** — a hand-written fixture drifts from real model output and silently stops testing anything.

- Model: `claude-sonnet-5` (`MODEL` env)
- Document set: `documents/acme-corp/`
- Iterations recorded: 2

Re-record when the prompt changes, `ApplicationFields` changes, or the document set changes. Review the diff.
